{
  "name": "d-<%= _.slugify(name) %>",
  "description": "Example bar chart Derby component with pure js",
  "version": "0.0.0",
  "main": "index.js",
  "keywords": [
    "derby",
    "component",
    "datavis"
  ],
  "scripts": {
    "test": "echo 'No tests yet!'; exit(1);"
  },
  "author": {
    "name": "<%= username %>",
    "email": "<%= email %>"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "peerDependencies": {
    <% if (stylus) { %>"derby-jade": "*",<% } %>
    <% if (jade) { %>"derby-stylus": "*",<% } %>
    <% if (coffee) { %>"coffee-script": "*",<% } %>
    "derby": ">= 0.6.0 < 0.7"
  }
}